Vehicle routing problem solving using genetic algorithms

To build application from source:
1. Install Node.js
    https://nodejs.org/en/

2. Install ElectronJS
    $> npm i electron -g

3. Install TypeScript
    $> npm i typescript -g

4. Install project dependencies
    $> npm i

5. Build & run
    $> npm start
